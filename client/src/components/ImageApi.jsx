

export default function ImageApi() {

  // const key = process.env.REACT_APP_PEXEL_API_KEY;

  return (
    <div>
      {/* {key && (
        <p>This is the API key: {key}</p>
      )} */}
      what up
    </div>
  )
}